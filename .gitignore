# Prawduct session files (TC dogfoods itself with Prawduct)
.claude/settings.local.json
.prawduct/.critic-findings.json
.prawduct/.session-git-baseline
.prawduct/.session-reflected
.prawduct/.session-start
.prawduct/.subagent-briefing.md
.prawduct/reflections.md
.prawduct/sync-manifest.json
__pycache__/

# TC's own project config (dogfooding artifact, not for distribution)
.tangleclaw/project.json

# Internal design doc (Prawduct intake artifact)
INTAKE-BRIEF.md

# Unreleased methodology templates (kept locally, not distributed)
data/templates/tilt/

# Uploads (user-specific)
.uploads/

# TLS certificates (generated locally per-host, contain private keys)
data/certs/

# Node
node_modules/

# TangleClaw runtime (user-specific, not in repo)
# ~/.tangleclaw/ is the runtime data directory
