{
  "_comment": "TB-1 (#357) orchestration profiles. Seeded once into ~/.tangleclaw/orchestration-profiles.json on first boot, then OPERATOR-OWNED (TC never overwrites your edits). Each profile maps to (baseUrl, model, keyRef). A project binds to a profile name via its `orchestrationProfile` field; at launch TC injects OPENAI_API_BASE/OPENAI_API_KEY (env) + --model (arg) for that launch only. Endpoints MUST use Tailscale MagicDNS names, never raw IPs. keyRef is a reference (file:<path> or env:<NAME>) resolved at launch — the secret is never stored here. A null baseUrl marks a not-yet-landed endpoint: the profile is selectable but refuses to inject (honest degradation, no silent fallback).",
  "_setup": "SHIPPED WITH NO ENDPOINTS BY DESIGN. Every baseUrl below is null, so these profiles are selectable but refuse to inject until you point them at your own inference host — that refusal is the intended first-run behavior, not a defect. Set baseUrl to your own Tailscale MagicDNS endpoint (e.g. http://<your-host>.<your-tailnet>.ts.net:4000/v1) and keyRef to wherever you keep that endpoint's key. The model names are examples; change them to whatever your endpoint serves. Edit the seeded copy at ~/.tangleclaw/orchestration-profiles.json — not this bundled template.",
  "profiles": {
    "direct": {
      "baseUrl": null,
      "model": "openai/qwen2.5-coder-32b-fp16",
      "keyRef": "file:~/.config/tangleclaw/orchestration.key",
      "status": "provisional"
    },
    "smart-fallback": {
      "baseUrl": null,
      "model": "openai/smart-code",
      "keyRef": "file:~/.config/tangleclaw/orchestration.key",
      "status": "provisional"
    },
    "semantic-route": {
      "baseUrl": null,
      "model": "openai/auto",
      "keyRef": "file:~/.config/tangleclaw/orchestration.key",
      "status": "provisional"
    }
  }
}
